<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>MultiplyEase — Table Generator</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      background: linear-gradient(to right, #f3e7ff, #e0c3fc);
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    .navbar {
      background-color: #6f42c1;
    }

    .navbar-brand {
      color: #fff !important;
      font-weight: bold;
      font-size: 24px;
    }

    .container {
      max-width: 700px;
      margin-top: 40px;
      margin-bottom: 40px;
    }

    .form-container {
      padding: 30px;
      background: #ffffff;
      border-radius: 16px;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
    }

    .form-title {
      font-weight: bold;
      color: #6f42c1;
      margin-bottom: 10px;
    }

    .subtitle {
      font-size: 16px;
      color: #888;
      margin-bottom: 20px;
    }

    .image-container {
      display: flex;
      justify-content: center;
      margin-bottom: 15px;
    }

    .image-container img {
      width: 140px;
      height: 140px;
      border-radius: 50%;
      object-fit: cover;
      border: 4px solid #6f42c1;
      box-shadow: 0 6px 12px rgba(0, 0, 0, 0.2);
    }

    .btn-primary {
      background-color: #6f42c1;
      border-color: #6f42c1;
      font-weight: bold;
    }

    .btn-primary:hover {
      background-color: #5a379e;
      border-color: #5a379e;
    }

    .table thead {
      background-color: #6f42c1;
      color: #fff;
    }

    .alert-info {
      background-color: #e6ccff;
      color: #4b0082;
      font-weight: bold;
    }

    footer {
      background-color: #6f42c1;
      color: white;
      padding: 15px 0;
      text-align: center;
      font-size: 15px;
      margin-top: auto;
    }
  </style>
</head>
<body>

  <nav class="navbar navbar-expand-lg">
    <div class="container-fluid px-4">
      <a class="navbar-brand" href="#">MultiplyEase</a>
    </div>
  </nav>

  <div class="container">
    <div class="form-container text-center">
      <div class="image-container">
        <img src="/image.png" alt="MultiplyEase Logo" />
      </div>

      <h2 class="form-title">Multiplication Table Generator</h2>
      <div class="subtitle">Powered by EJS + Bootstrap</div>

      <form action="/about" method="POST" class="text-start">
        <div class="mb-3">
          <label for="num" class="form-label">Enter a number:</label>
          <input type="number" id="num" name="num" class="form-control" placeholder="e.g. 5" required value="<%= num || '' %>" />
        </div>

        <div class="mb-3">
          <label for="limit" class="form-label">Enter limit:</label>
          <input type="number" id="limit" name="limit" class="form-control" placeholder="e.g. 10" required value="<%= limit || 10 %>" />
        </div>

        <button type="submit" class="btn btn-primary btn-lg w-100 mt-3">✨ Generate Table</button>
      </form>

      <% if (message) { %>
        <div class="alert alert-info text-center mt-4">
          <%= message %>
        </div>

        <% if (tableData.length > 0) { %>
          <div class="table-responsive mt-3">
            <table class="table table-bordered table-striped text-center">
              <thead>
                <tr>
                  <th>Expression</th>
                  <th>Result</th>
                </tr>
              </thead>
              <tbody>
                <% tableData.forEach(row => { %>
                  <tr>
                    <td><%= num %> × <%= row.i %></td>
                    <td><%= row.result %></td>
                  </tr>
                <% }) %>
              </tbody>
            </table>
          </div>
        <% } %>
      <% } %>
    </div>
  </div>


  <footer>
    &copy; <%= new Date().getFullYear() %> MultiplyEase by Ayesha Malak. All rights reserved.
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
